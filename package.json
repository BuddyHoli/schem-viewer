{
  "name": "schem-viewer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:android": "npm run build && npx cap copy android"
  },
  "dependencies": {
    "prismarine-nbt": "^2.5.2",
    "three": "^0.152.2",
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/cli": "^5.0.0"
  }
}